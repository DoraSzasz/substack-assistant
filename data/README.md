# data/

This directory holds **your personal** voice profile and content index.

After running `python substack_assist.py setup`, you'll see:

- `voice_profile.json` — your extracted writing voice
- `content_index.json` — your indexed article archive

These files are **gitignored** on purpose. They're personal to you and
shouldn't be committed to a public repo. Every user generates their own.
